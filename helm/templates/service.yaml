apiVersion: v1
kind: Service
metadata:
  name: {{ include "hasura-cache.fullname" . }}
  labels:
    {{- include "hasura-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hasura-cache.selectorLabels" . | nindent 4 }}
